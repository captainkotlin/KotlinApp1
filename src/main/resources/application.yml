server:
  port: 8081

datasource:
  driverClassName: org.postgresql.Driver
  jdbcUrl: jdbc:postgresql://localhost:5432/Kotlin3
  username: postgres
  password: postgres

hibernate:
  dialect: org.hibernate.dialect.PostgreSQLDialect
  hbm2ddl.auto: update
  show_sql: true
  format_sql: true